name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a script
        run: ls
      - uses: actions/checkout@v4
      - name: run second script
        run: hostname
        